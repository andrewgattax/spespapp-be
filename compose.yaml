services:
  plotfail:
    build: .
    container_name: spespapp-be
    restart: unless-stopped
    ports:
      - "8000:8080"
    environment:
      # Database Configuration
      - DB_HOST=100.118.208.209:3306
      - DB_NAME=spespapp
      - DB_USERNAME=spespapp
      - DB_PASSWORD=spespapp
      # Redis Configuration
      - REDIS_HOST=localhost
      - REDIS_PORT=6379
      - REDIS_PASSWORD=
      # Security Configuration
      - JWT_SECRET=ecd5e0dc314bfd9b147fb06eb3edae7f477fa4eb82eae922af0aeb90aaec451a
      - JWT_EXPIRATION_MS=604800000 # 7 days in milliseconds
      # Server Configuration
      - SERVER_PORT=8080

